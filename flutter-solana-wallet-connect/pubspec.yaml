name: test5
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Solana & Crypto
  solana: ^0.31.2+1
  solana_mobile_client: ^0.1.2
  cryptography: ^2.7.0
  pinenacl: ^0.6.0  # ✅ Added: NaCl for Phantom decryption

  # UI & Navigation
  app_links: ^6.4.0
  url_launcher: ^6.3.2

  # Utilities
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true